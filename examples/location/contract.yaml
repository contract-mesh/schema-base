type: program/1.0.0
info:
    org: ellie-mae/architecture
    name: samples/location-service
features:
    openapi:
        - $ref: ../standard/rest-endpoints.yaml
        - $ref: rest.yaml
    asyncapi:
        - $ref: asyncapi.yaml