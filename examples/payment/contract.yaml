type: program/1.0.0
info:
    org: ellie-mae/architecture
    name: samples/payment-service
features:
    openapi:
        - $ref: ../standard/rest-endpoints.yaml
    asyncapi:
        - $ref: asyncapi.yaml
dependencies:
    ../catalog:
        asyncapi:
            - event/catalog/order/stockConfirmed